# CS559 Portfolio

To view, you need to run a local web server and start with the `portfolio.html` page. One possible method is using the Live Server extension on VScode. The html file will not work without a local server.
