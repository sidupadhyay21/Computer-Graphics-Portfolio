# CS559 Portfolio from Spring 2025

My two biggest projects in the Computer Graphics class. To view, you need to run a local web server and start with the `portfolio.html` page. One possible method is using the Live Server extension on VScode. The html file will not work without a local server.